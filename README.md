```
bash <(curl -Ls https://raw.githubusercontent.com/umenuan/ssh/main/ssh.sh)
```
```
apt update && apt install curl -y && bash <(curl -Ls https://raw.githubusercontent.com/umenuan/ssh/main/ssh.sh)
```
